My cli tool for restoring installed applications when reinstalling the OS.

### Installation
```bash
sudo bash -c 'bash <(wget -qO- https://raw.githubusercontent.com/helightdev/hrestore/refs/heads/main/install.sh)'
```