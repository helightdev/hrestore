name: hrestore
description: A sample command-line application with basic argument parsing.
version: 0.0.1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.3

executables:
  hrestore:

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  mason_logger: ^0.3.3
  toml: ^0.17.0
  io: ^1.0.5
  dart_scope_functions: ^1.0.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
